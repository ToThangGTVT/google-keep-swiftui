# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'keep' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
  pod 'GTMAppAuth'

  # Pods for keep

  target 'keepTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'keepUITests' do
    # Pods for testing
  end

end
